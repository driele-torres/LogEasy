<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <shape>
            <gradient
                android:startColor="@color/AzulClaro"
                android:centerColor="@color/AzulClaro"
                android:centerY="1.0"
                android:endColor="@color/AzulClaro"
                android:angle="270"
                />
        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:startColor="@color/AzulBright"
                    android:centerColor="@color/AzulBright"
                    android:centerY="1.0"
                    android:endColor="@color/AzulBright"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>
</layer-list>


    <!--        android:progressTint="@color/Laranja"
        android:progressBackgroundTint="@color/Rosa"
        -->